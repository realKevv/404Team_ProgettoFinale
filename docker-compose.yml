services:
  mysql:
    image: mysql:8.4
    container_name: business-travel-db

    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: businesstravel

    ports:
      - "3306:3306"

    volumes:
      - mysql_data:/var/lib/mysql

    healthcheck:
      test: [ "CMD", "mysqladmin", "ping", "-h", "localhost" ]
      interval: 10s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    container_name: business-travel-api

    ports:
      - "5000:5000"

    environment:
      DB_HOST: mysql
      DB_PORT: "3306"
      DB_USER: root
      DB_PASSWORD: root
      DB_NAME: businesstravel

    depends_on:
      - mysql

    volumes:
      - ./backend:/app
      - /app/node_modules

  frontend:
    build: ./frontend
    container_name: business-travel-web

    ports:
      - "5173:5173"

    depends_on:
      - backend

    volumes:
      - ./frontend:/app
      - /app/node_modules

volumes:
  mysql_data:
